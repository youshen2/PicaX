enum WatchSettingsKey {
    static let defaultExplorePlatform = "watch.explore.defaultPlatform"
    static let showsAllExplorePlatforms = "watch.explore.showsAllPlatforms"
    static let defaultTagsPlatform = "watch.tags.defaultPlatform"
    static let maxVisibleComics = "watch.lists.maxVisibleComics"
}
